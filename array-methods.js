let arr = [1,2,3,4]

console.log(arr)  // [1,2,3,4]

push() adds item to the end 
 
arr.push(5) 

console.log(arr) // [1,2,3,4,5]

// adds items to the end and returns the length
// pop() removes the last element of the array and returns that removed element
// shift() removes the first element of the array and returns it

